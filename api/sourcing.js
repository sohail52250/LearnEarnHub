const service=require('../services/sourcing-service');
module.exports=async function(req,res){try{if(req.method==='GET'){const action=String(req.query?.action||'suppliers').toLowerCase();if(action==='suppliers'){const suppliers=await service.listSuppliers({limit:req.query?.limit});return res.status(200).json({success:true,suppliers,count:suppliers.length})}if(action==='my-requests'){const requests=await service.listMyRequests(req);return res.status(200).json({success:true,requests,count:requests.length})}return res.status(400).json({success:false,error:'Invalid sourcing action.'})}if(req.method!=='POST')return res.status(405).json({success:false,error:'Method not allowed.'});const b=req.body||{},a=String(b.action||'create-request').toLowerCase();if(a==='create-request'){const request=await service.createRequest(req,b);return res.status(201).json({success:true,request})}if(a==='create-quote'){const quotation=await service.createQuote(req,b);return res.status(201).json({success:true,quotation})}return res.status(400).json({success:false,error:'Invalid sourcing action.'})}catch(e){console.error('SOURCING API ERROR:',e);return res.status(e.status||500).json({success:false,error:e.message||'Sourcing operation failed.'})}};
